import { createTheme } from "@mui/material/styles";

export const appTheme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#5893df",
    },
    secondary: {
      main: "#f50057",
    },
  },
});
